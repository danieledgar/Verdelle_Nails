[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "bash -lc 'cd backend && python3 manage.py migrate && python3 populate_services.py && gunicorn verdelle_nails.wsgi:application --bind 0.0.0.0:$PORT'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
